{
  "name": "lulu-road-trip",
  "version": "1.0.0",
  "private": true,
  "description": "Lulu's Road Trip — native iOS wrapper (Capacitor) for the web game at lulu.boats.",
  "scripts": {
    "build": "node build.js",
    "check": "node build.js --check",
    "copy:web": "node scripts/copy-web.js",
    "serve": "npx http-server -p 8099 -c-1",
    "add:ios": "npm run build && npm run copy:web && npx cap add ios",
    "sync:ios": "npm run build && npm run copy:web && npx cap sync ios",
    "open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor-community/admob": "^8.0.0",
    "@capacitor/app": "^8.1.0",
    "@capacitor/core": "^8.3.4",
    "@capacitor/ios": "^8.3.4"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^8.3.4"
  }
}
