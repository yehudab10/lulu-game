node_modules/

# Capacitor — generated, not source. The native iOS project and the staged web
# bundle are regenerated on every build (locally via `npm run add:ios`, in CI
# via codemagic.yaml), so they don't belong in git.
www/
ios/
android/

# Claude Code harness state (agent worktrees, session data) — not source.
.claude/
